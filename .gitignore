# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# Dependencies (if any get added later)
node_modules/

# Local env / secrets
.env
.env.local

# Original full-size PNG masters (kept locally; site serves optimized .webp)
_originals/
